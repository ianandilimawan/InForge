@extends('admin.layouts.app')

@section('title', '{{MODEL_NAME_PLURAL}}')

@section('content')
    <div class="space-y-6">

<!-- Page Header -->
        <div class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-4">
            <div>
                <h1 class="text-base sm:text-lg md:text-xl font-bold tracking-tight text-zinc-900 dark:text-white">{{MODEL_NAME_PLURAL}}</h1>
                <p class="text-xs text-zinc-500 dark:text-zinc-400 mt-0.5">Manage your {{MODEL_NAME_LOWER_PLURAL}} here</p>
            </div>
            <div class="flex flex-wrap gap-3">
                @if(auth()->user() && auth()->user()->hasPermission('create-{{MODEL_NAME_SNAKE_PLURAL}}'))
                {{IMPORT_BUTTON}}
                <x-button href="{{ route('{{ROUTE_NAME}}.create') }}" variant="primary" :solid="true" icon="plus">
                    Add {{MODEL_NAME_TITLE}}
                </x-button>
                @endif
            </div>
        </div>

        <!-- DataTable -->
        <livewire:tables.{{LIVEWIRE_TABLE_TAG}} />
    </div>

    <!-- Delete Confirmation Modal -->
    <x-confirm-delete-modal title="Delete {{MODEL_NAME_TITLE}}"
        message="Are you sure you want to delete this {{MODEL_NAME_LOWER}}? This action cannot be undone." />
@endsection
